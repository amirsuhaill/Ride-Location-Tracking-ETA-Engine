fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1

# ETA model (Phase 9): DB access, feature engineering, training, and persisted-model serving.
# tzdata is required explicitly (not just left as a transitive dependency of pandas) because
# app/ml/constants.py's zoneinfo.ZoneInfo("America/Los_Angeles") needs an IANA tz database, and
# the python:3.12-slim base image (Dockerfile) doesn't ship the system one.
tzdata==2026.3
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
joblib==1.4.2

# Real road-network routing via OSRM (Phase 15, see docs/osrm-routing.md) — used by
# scripts/train_model.py, so it must be a production dependency, not dev-only.
httpx==0.27.2
